# 🎮 Руководство по интерактивным упражнениям

## 📋 Обзор

Система интерактивных упражнений позволяет студентам выполнять Python код прямо в браузере с автоматической проверкой через Pyodide. 

✅ **Pyodide полностью реализован и готов к использованию!**

## 🚀 Быстрый старт

### Базовое использование

```markdown
{{ code_input_form(
    exercise_id="unique_exercise_id",
    initial_code="def hello():\n    return 'Hello World'",
    placeholder="Введите ваш код..."
) }}
```

### С автоматической проверкой

```markdown
{{ code_input_form(
    exercise_id="exercise_01",
    initial_code="def add(a, b):\n    # Ваш код здесь\n    pass",
    placeholder="Реализуйте функцию add",
    use_pyodide=True,
    test_cases=[
        {
            "code": "assert add(2, 3) == 5, '2 + 3 должно быть 5'",
            "description": "Проверка сложения положительных чисел"
        },
        {
            "code": "assert add(-1, 1) == 0, '-1 + 1 должно быть 0'",
            "description": "Проверка сложения отрицательных чисел"
        }
    ]
) }}
```

## 📝 Параметры функции

### `code_input_form()`

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `exercise_id` | `str` | ✅ Да | Уникальный идентификатор упражнения |
| `initial_code` | `str` | ❌ Нет | Начальный код в редакторе |
| `placeholder` | `str` | ❌ Нет | Текст подсказки в пустом редакторе |
| `use_pyodide` | `bool` | ❌ Нет | Использовать Pyodide для выполнения (по умолчанию `True`) |
| `test_cases` | `list` | ❌ Нет | Список тестовых случаев |

### Структура `test_cases`

```python
test_cases = [
    {
        "code": "assert function_name(1, 2) == 3",
        "description": "Описание теста"
    }
]
```

## 🎯 Примеры использования

### Пример 1: Простое упражнение

```markdown
{{ code_input_form(
    exercise_id="hello_world",
    initial_code="def greet(name):\n    # Напишите функцию приветствия\n    pass",
    placeholder="Реализуйте функцию greet(name)"
) }}
```

### Пример 2: С тестами

```markdown
{{ code_input_form(
    exercise_id="calculator",
    initial_code="def multiply(a, b):\n    pass",
    test_cases=[
        {
            "code": "assert multiply(2, 3) == 6",
            "description": "Умножение положительных чисел"
        },
        {
            "code": "assert multiply(-2, 3) == -6",
            "description": "Умножение с отрицательным числом"
        },
        {
            "code": "assert multiply(0, 5) == 0",
            "description": "Умножение на ноль"
        }
    ]
) }}
```

### 🔴 Advanced: Пример 3: SOLID упражнение

```markdown
{{ code_input_form(
    exercise_id="srp_exercise",
    initial_code="""from typing import Protocol

# Создайте протокол для валидации
class Validator(Protocol):
    def validate(self, data: dict) -> bool:
        ...

# Реализуйте класс EmailValidator
class EmailValidator:
    pass
""",
    test_cases=[
        {
            "code": """
validator = EmailValidator()
assert validator.validate({"email": "test@example.com"}) == True
assert validator.validate({"email": "invalid"}) == False
            """,
            "description": "EmailValidator корректно валидирует email"
        }
    ]
) }}
```

## 🔧 Технические детали

### Как это работает

1. **Pyodide** загружается из CDN при первом использовании (автоматически)
2. Код пользователя выполняется в изолированной среде браузера
3. Тесты запускаются автоматически после выполнения кода
4. Результаты отображаются в реальном времени с детальной информацией
5. Подсказки и рекомендации помогают исправить ошибки

### ✅ Реализованные возможности

- ✅ Выполнение Python кода в браузере через Pyodide
- ✅ Автоматическая проверка тестов
- ✅ Детальные сообщения об ошибках
- ✅ Подсказки для исправления ошибок
- ✅ Поддержка множественных тестовых случаев
- ✅ Визуальная обратная связь (успех/ошибка)
- ✅ Безопасное выполнение (изоляция от системы)

### Ограничения

- ⚠️ Pyodide не поддерживает все стандартные библиотеки Python
- ⚠️ Некоторые библиотеки нужно устанавливать через `micropip`
- ⚠️ Выполнение происходит в браузере, поэтому производительность ограничена

#### Ограничения времени выполнения

- **Совместимость браузеров**: Минимальные поддерживаемые версии — Chrome 90+, Firefox 88+, Safari 14+, Edge 90+; обновите браузер до последней версии для оптимальной производительности
- **Таймаут выполнения**: Максимальное время выполнения кода — 30 секунд на одно упражнение; разбивайте длительные задачи на более мелкие части
- **Лимиты памяти**: Приблизительный лимит памяти для выполнения в браузере — 256–512 МБ в зависимости от устройства; избегайте создания больших массивов данных или используйте потоковую обработку
- **Производительность**: Выполнение через Pyodide в 2–5 раз медленнее, чем нативный Python; ожидайте более медленное выполнение для вычислительно сложных операций

### Поддерживаемые библиотеки

По умолчанию доступны:
- Стандартная библиотека Python
- `dataclasses`
- `typing`
- `collections`
- `itertools`

Для установки дополнительных библиотек:

```python
# В коде упражнения можно использовать:
import micropip

# ⚠️ ВАЖНО: await micropip.install() требует асинхронного контекста выполнения
# Это работает в Pyodide/web-worker/browser JS контексте, а не в обычном CPython

# Вариант 1: Использование в async функции (рекомендуется)
async def setup():
    await micropip.install("numpy")
    import numpy as np
    # Ваш код здесь

# Вариант 2: Top-level await (где поддерживается)
await micropip.install("numpy")
import numpy as np
```

!!! note "Асинхронный контекст"
    `await micropip.install()` работает только в асинхронном контексте. Если вы вызываете код из синхронного host-кода JavaScript, оберните установку в `async` функцию или используйте Pyodide's `runPythonAsync()`. Подробнее: [Pyodide: Loading packages](https://pyodide.org/en/stable/usage/loading-packages.html)

## 💡 Советы по созданию упражнений

### 1. Начинайте с простого

```python
# ✅ Хорошо: Простое начало
initial_code = "def add(a, b):\n    pass"

# ❌ Плохо: Слишком сложно сразу
initial_code = """
from typing import Protocol, Generic, TypeVar
from dataclasses import dataclass
# ... много кода
"""
```

### 2. Добавляйте полезные комментарии

```python
initial_code = """
# TODO: Реализуйте функцию согласно SRP
# 1. Создайте класс UserValidator
# 2. Создайте класс UserRepository  
# 3. Создайте класс UserService
"""
```

### 3. Пишите понятные тесты

```python
test_cases = [
    {
        "code": "assert result == expected",
        "description": "Короткое и понятное описание"
    }
]
```

### 4. Предоставляйте подсказки

```markdown
!!! tip "Подсказка"
    Используйте Protocol для создания интерфейсов
```

## 🐛 Отладка

### Проблема: Pyodide не загружается

**Решение**: Проверьте подключение к интернету. Pyodide загружается с CDN.

### Проблема: Код не выполняется

**Решение**: 
1. Проверьте синтаксис Python
2. Убедитесь, что используются только поддерживаемые библиотеки
3. Проверьте консоль браузера на ошибки

### Проблема: Тесты не проходят

**Решение**:
1. Проверьте логику тестов
2. Убедитесь, что код студента соответствует требованиям
3. Добавьте больше описательных сообщений в assert

## 📚 Дополнительные ресурсы

- [Pyodide Documentation](https://pyodide.org/)
- [MkDocs Macros Plugin](https://mkdocs-macros-plugin.readthedocs.io/)

---

!!! tip "Лучшие практики"
    - Используйте уникальные `exercise_id` для каждого упражнения
    - Предоставляйте начальный код с комментариями
    - Пишите понятные описания тестов
    - Добавляйте подсказки для сложных заданий

