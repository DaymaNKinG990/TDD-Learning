# 🎯 Урок 5: Interface Segregation Principle (ISP)

## 🎯 Цели урока

После изучения этого урока вы сможете:
- ✅ Распознавать "толстые" интерфейсы в коде
- ✅ Разделять интерфейсы на специализированные
- ✅ Создавать клиенто-специфические интерфейсы
- ✅ Применять ISP в реальных проектах

## 📖 Определение ISP

!!! quote "Принцип разделения интерфейсов"
    **Клиенты не должны зависеть от методов, которые они не используют. Лучше иметь много специализированных интерфейсов, чем один универсальный.**

### 💡 Простыми словами:

Представьте универсальный пульт от всего:
- ❌ **Плохо**: Пульт с 100 кнопками (TV, кондиционер, стиралка, микроволновка...)
- ✅ **Хорошо**: Отдельные пульты для каждого устройства с нужными кнопками

В программировании:
- **Не заставляйте класс реализовывать методы, которые ему не нужны**
- **Лучше несколько маленьких интерфейсов, чем один большой**
- **Каждый клиент использует только то, что ему нужно**

### Почему это важно?

**Проблема толстых интерфейсов:**
```python
# ❌ Плохо: робот вынужден реализовывать человеческие методы
class Worker(Protocol):
    def work(self): ...
    def eat(self): ...      # Роботу не нужно!
    def sleep(self): ...    # Роботу не нужно!

class Robot(Worker):
    def eat(self):
        raise NotImplementedError("Роботы не едят!")  # 💥 Проблема!
```

**Преимущества разделения:**
```python
# ✅ Хорошо: робот использует только нужный интерфейс
class Workable(Protocol):
    def work(self): ...

class Robot(Workable):
    def work(self): ...  # Только то, что нужно!
```

**Результат:**
- Четкая специализация интерфейсов
- Легче тестировать и поддерживать
- Улучшается читаемость кода
- Снижается связанность

## 🔍 Как распознать нарушение ISP?

### Признаки толстого интерфейса

1. **Много абстрактных методов** (> 5-7 методов)
2. **NotImplementedError** в реализациях
3. **Методы с префиксами** (get_, set_, process_, validate_)
4. **Разные клиенты используют разные методы**
5. **Интерфейс пытается быть универсальным**

## ❌ Пример нарушения ISP

### "Толстый" интерфейс работника

```python
from typing import Protocol

class Worker(Protocol):
    """❌ Толстый интерфейс - заставляет реализовывать ненужные методы"""

    def work(self) -> str: ...

    def eat(self) -> str: ...

    def sleep(self) -> str: ...

    def get_salary(self) -> float: ...

    def code(self) -> str: ...

    def design(self) -> str: ...

class Programmer:
    """❌ Вынужден реализовывать все методы, даже ненужные"""

    def work(self) -> str:
        return "Программист работает"

    def eat(self) -> str:
        return "Программист ест"

    def sleep(self) -> str:
        return "Программист спит"

    def get_salary(self) -> float:
        return 5000.0

    def code(self) -> str:
        return "Программист пишет код"

    def design(self) -> str:
        # ❌ Вынужден реализовывать, хотя не умеет!
        raise NotImplementedError("Программист не дизайнер")

class Robot:
    """❌ Роботу не нужны человеческие потребности"""

    def work(self) -> str:
        return "Робот работает 24/7"

    def eat(self) -> str:
        # ❌ Роботы не едят!
        raise NotImplementedError("Роботы не едят")

    def sleep(self) -> str:
        # ❌ Роботы не спят!
        raise NotImplementedError("Роботы не спят")

    def get_salary(self) -> float:
        # ❌ Роботы не получают зарплату!
        raise NotImplementedError("Роботы не получают зарплату")

    def code(self) -> str:
        return "Робот генерирует код"

    def design(self) -> str:
        return "Робот создает UI по шаблонам"
```

### Анализ проблем

| Проблема | Последствия |
|----------|-------------|
| **NotImplementedError** | Исключения в runtime |
| **Толстый интерфейс** | Сложно понять и поддерживать |
| **Принудительная реализация** | Методы, которые не нужны |
| **Тестирование** | Нужно тестировать исключения |

## ✅ Правильное применение ISP

### 🎯 Анализ: кто что использует?

```
HR система:     eat, sleep, get_salary  <- Человеческие потребности
Проект менеджер: work, code, design     <- Рабочие активности  
Робот система:  work, code              <- Только автоматизация

Вывод: Нужны РАЗНЫЕ интерфейсы для РАЗНЫХ клиентов!
```

### 📋 Создание специализированных интерфейсов

```python
from typing import Protocol

class HumanNeeds(Protocol):
    """Только для людей"""
    def eat(self) -> str: ...
    def sleep(self) -> str: ...
    def get_salary(self) -> float: ...

class WorkActivities(Protocol):
    """Рабочие задачи"""
    def work(self) -> str: ...
    def code(self) -> str: ...
    def design(self) -> str: ...

class RobotCapabilities(Protocol):
    """Только для роботов"""
    def work(self) -> str: ...
    def code(self) -> str: ...
```

### 🔧 Правильные реализации

```python
class Programmer(HumanNeeds, WorkActivities):
    """✅ Программист реализует только то, что нужно"""
    
    # Человеческие потребности
    def eat(self) -> str:
        return "Программист ест пиццу"
    
    def sleep(self) -> str:
        return "Программист спит 8 часов"
    
    def get_salary(self) -> float:
        return 5000.0
    
    # Рабочие активности
    def work(self) -> str:
        return "Программист разрабатывает"
    
    def code(self) -> str:
        return "Программист пишет Python"
    
    def design(self) -> str:
        return "Программист создает архитектуру"

class Robot(RobotCapabilities):
    """✅ Робот реализует только свои способности"""
    
    def work(self) -> str:
        return "Робот работает 24/7"
    
    def code(self) -> str:
        return "Робот генерирует код по AI"
```

### 🎯 Клиентский код становится простым

```python
# HR система знает только о людях
def hr_system(employee: HumanNeeds) -> None:
    print(f"Зарплата: {employee.get_salary()}")
    print(employee.eat())

# Проектная система знает только о работе  
def project_system(worker: WorkActivities) -> None:
    print(worker.work())
    print(worker.code())

# Роботы управляются отдельно
def robot_system(robot: RobotCapabilities) -> None:
    print(robot.work())
    print(robot.code())

# Использование
programmer = Programmer()
robot = Robot()

hr_system(programmer)      # ✅ Работает
project_system(programmer) # ✅ Работает
robot_system(robot)       # ✅ Работает

# hr_system(robot)        # ❌ Type checking error — robot is not a human!
```

## 🎯 Ключевые стратегии ISP

### 1. **Разделение по клиентам**

```python
from typing import Protocol

# ❌ До: один толстый интерфейс
class Repository(Protocol):
    def get_by_id(self, id: int) -> object: ...
    def save(self, entity: object) -> None: ...
    def delete(self, id: int) -> None: ...
    def get_stats(self) -> dict: ...

# ✅ После: разделение по потребностям
class ReadableRepository(Protocol):
    def get_by_id(self, id: int) -> object: ...

class WritableRepository(Protocol):
    def save(self, entity: object) -> None: ...
    def delete(self, id: int) -> None: ...

# Клиенты используют только нужное
class ReportService:
    def __init__(self, repo: ReadableRepository):  # Только чтение
        self.repo = repo

class DataService:
    def __init__(self, repo: WritableRepository):  # Только запись
        self.repo = repo
```

### 2. **Композиция интерфейсов**

```python
# Строим сложные интерфейсы из простых

class BasicLogger(Protocol):
    def log_info(self, message: str) -> None: ...
    def log_error(self, message: str) -> None: ...

class AdvancedLogger(BasicLogger):
    def log_debug(self, message: str) -> None: ...

# Клиенты выбирают нужный уровень
class SimpleService:
    def __init__(self, logger: BasicLogger):  # Минимум
        self.logger = logger

class AdvancedService:  
    def __init__(self, logger: AdvancedLogger):  # Расширенный
        self.logger = logger
```

## 🎮 Практические упражнения

### Упражнение 1: Анализ толстого интерфейса

**Задание:** Возьмите следующий интерфейс и разделите его на специализированные интерфейсы.

```python
from typing import Protocol

class MediaProcessor(Protocol):
    def process_video(self, file: str) -> None: ...
    def process_audio(self, file: str) -> None: ...
    def process_image(self, file: str) -> None: ...
    def compress_video(self, file: str) -> None: ...
    def compress_audio(self, file: str) -> None: ...
    def compress_image(self, file: str) -> None: ...
    def extract_metadata(self, file: str) -> dict: ...
    def validate_file(self, file: str) -> bool: ...
    def upload_to_cloud(self, file: str) -> str: ...
```

**💡 Подсказки для решения:**
> **Анализ групп методов:** Видите ли вы группы методов по функциональности?
>
> **Нужно создать:**
> - 📋 **3 Protocol**: `MediaProcessor`, `MediaCompressor`, `MediaUploader`
> - 🔧 **3 специализации**: `VideoProcessor`, `AudioProcessor`, `ImageProcessor`
> - 🎯 **1 комбинированный**: `FullMediaService` для клиентов, которым нужно всё
>
> **Принцип ISP:** Каждый клиент использует только нужные ему методы

### Упражнение 2: Создание клиент-специфических интерфейсов

**Задание:** Создайте интерфейсы для разных клиентов системы электронной коммерции.

Клиенты системы:
1. **Покупатель** - просмотр товаров, корзина, заказы
2. **Продавец** - управление товарами, аналитика продаж
3. **Администратор** - управление пользователями, система
4. **Служба доставки** - обновление статуса доставки

**💡 Подсказки для решения:**
> **Анализ по ролям:** Каждый клиент выполняет РАЗНЫЕ операции
>
> **Нужно создать:**
> - 📋 **4 Protocol**: `CustomerOperations`, `SellerOperations`, `AdminOperations`, `DeliveryOperations`
> - 🔧 **4 реализации**: под каждую роль пользователя
> - 🎯 **Композитные интерфейсы**: для пользователей с несколькими ролями
>
> **Принцип:** Интерфейс = роль пользователя в системе

### Упражнение 3: Рефакторинг существующего кода

**Задание:** Возьмите класс из вашего проекта и примените ISP.

```python
from typing import Protocol

class UserService:
    def create_user(self, data): ...
    def update_user(self, user_id, data): ...
    def delete_user(self, user_id): ...
    def get_user(self, user_id): ...
    def get_all_users(self): ...
    def authenticate_user(self, credentials): ...
    def change_password(self, user_id, password): ...
    def send_password_reset(self, email): ...
    def verify_email(self, token): ...
    def update_profile(self, user_id, profile): ...
    def get_user_orders(self, user_id): ...
    def get_user_reviews(self, user_id): ...
```

**💡 Подсказки для решения:**
> **Группировка методов:** Найдите группы по бизнес-функциям
>
> **Нужно создать:**
> - 📋 **4 Protocol**: `UserRepository`, `AuthService`, `ProfileService`, `UserDataService`
> - 🔧 **4 сервиса**: каждый отвечает за свою область
> - 🎯 **UserFacade**: координатор для сложных операций
>
> **Принцип ISP:** Один интерфейс = одна бизнес-ответственность

## 🎯 Ключевые выводы

1. **ISP - ключ к гибким интерфейсам**
2. **Анализируйте клиентов интерфейса**
3. **Разделяйте интерфейсы по ответственности**
4. **Композиция лучше монолитных интерфейсов**
5. **Тестируемость - индикатор правильного разделения**

## 🚀 Следующие шаги

!!! success "Что вы узнали"
    - ✅ Клиенты не должны зависеть от методов, которые не используют
    - ✅ Лучше несколько маленьких интерфейсов, чем один большой
    - ✅ Разделение интерфейсов улучшает тестируемость

!!! tip "Практика"
    Найдите интерфейс с множеством методов. Проверьте, используют ли все клиенты все методы. Если нет - разделите интерфейс.

Теперь вы готовы изучить **[Принцип инверсии зависимостей](06-dependency-inversion.md)** - финальный принцип SOLID!

---

!!! tip "Практический совет"
    Задавайте вопрос: "Будет ли каждый клиент использовать все методы этого интерфейса?" Если ответ "нет" - разделите интерфейс.

## 🧪 Проверьте свои знания: ISP

<div class="quiz-container" id="isp-quiz">
<script type="application/json">
{
  "title": "Interface Segregation Principle",
  "description": "Проверьте понимание принципа разделения интерфейсов",
  "icon": "🎯",
  "questions": [
    {
      "question": "Что означает принцип Разделения Интерфейсов (ISP)?",
      "type": "single",
      "points": 1,
      "options": [
        {"text": "Клиенты не должны зависеть от методов, которые они не используют", "correct": true},
        {"text": "Все классы должны реализовывать один общий интерфейс", "correct": false},
        {"text": "Интерфейсы должны быть как можно больше", "correct": false},
        {"text": "Клиенты должны знать о всех методах интерфейса", "correct": false}
      ],
      "explanation": "ISP говорит о разделении толстых интерфейсов на специализированные"
    },
    {
      "question": "Какой интерфейс нарушает ISP?",
      "type": "single",
      "points": 1,
      "code": "# Вариант A: Специализированные интерфейсы\nclass Workable(Protocol):\n    def work(self) -> None: ...\n\nclass Eatable(Protocol):\n    def eat(self) -> None: ...\n\nclass Human(Workable, Eatable):\n    def work(self) -> None: pass\n    def eat(self) -> None: pass\n\n# Вариант B: Толстый интерфейс\nclass Worker(Protocol):\n    def work(self) -> None: ...\n    def eat(self) -> None: ...\n    def sleep(self) -> None: ...\n\nclass Robot(Worker):\n    def work(self) -> None: pass\n    def eat(self) -> None:\n        raise NotImplementedError()\n    def sleep(self) -> None:\n        raise NotImplementedError()",
      "options": [
        {"text": "Вариант A - специализированные интерфейсы", "correct": false},
        {"text": "Вариант B - толстый интерфейс", "correct": true}
      ],
      "explanation": "Robot вынужден реализовывать методы eat() и sleep(), которые ему не нужны"
    },
    {
      "question": "Что такое 'толстый интерфейс'?",
      "type": "single",
      "points": 1,
      "options": [
        {"text": "Интерфейс с большим количеством методов", "correct": true},
        {"text": "Интерфейс с длинными именами методов", "correct": false},
        {"text": "Интерфейс с большим количеством реализаций", "correct": false},
        {"text": "Интерфейс с большим количеством документации", "correct": false}
      ],
      "explanation": "Толстый интерфейс содержит много методов, которые не нужны всем клиентам"
    },
    {
      "question": "Какой подход лучше для соблюдения ISP?",
      "type": "single",
      "points": 1,
      "options": [
        {"text": "Разделить интерфейс на специализированные", "correct": true},
        {"text": "Добавить методы-заглушки (NotImplementedError)", "correct": false},
        {"text": "Объединить все интерфейсы в один универсальный", "correct": false},
        {"text": "Использовать множественное наследование", "correct": false}
      ],
      "explanation": "ISP достигается путем разделения интерфейсов по ответственностям клиентов"
    },
    {
      "question": "Что является главным преимуществом соблюдения ISP?",
      "type": "single",
      "points": 1,
      "options": [
        {"text": "Клиенты реализуют только нужные методы", "correct": true},
        {"text": "Уменьшается количество интерфейсов", "correct": false},
        {"text": "Увеличивается связность модулей", "correct": false},
        {"text": "Появляется один универсальный интерфейс", "correct": false}
      ],
      "explanation": "ISP позволяет клиентам реализовывать только необходимые им методы"
    }
  ]
}
</script>
</div>

