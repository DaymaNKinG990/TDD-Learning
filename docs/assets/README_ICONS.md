# 🎨 Иконки курса "Мастерство архитектуры и паттернов проектирования"

## 📋 Описание

Иконки отражают тематику курса:
- **Архитектура** - многослойная структура (Clean Architecture)
- **Паттерны проектирования** - геометрические фигуры (Singleton, Factory, Observer)
- **SOLID принципы** - 5 точек
- **Микросервисы** - маленькие связанные блоки
- **Буква "A"** - Architecture (архитектура)

## 🎨 Цветовая схема

- **Синий (#1e40af, #3b82f6)** - основной цвет, символизирует архитектуру
- **Желтый (#fbbf24)** - паттерны (Singleton)
- **Зеленый (#10b981)** - паттерны (Factory)
- **Красный (#ef4444)** - паттерны (Observer)
- **Фиолетовый (#8b5cf6)** - SOLID принципы
- **Розовый (#ec4899)** - микросервисы

## 📁 Файлы

### SVG иконка
- `favicon.svg` - векторная иконка для современных браузеров

### PNG иконки (созданы автоматически)
- `favicon-16x16.png` - для маленьких размеров
- `favicon-32x32.png` - стандартный размер
- `favicon-48x48.png` - для средних размеров
- `favicon-64x64.png` - для больших размеров
- `favicon-128x128.png` - для высокого разрешения
- `favicon-256x256.png` - для Apple Touch Icon и Android

### ICO файл
- `favicon.ico` - для старых браузеров (нужно создать вручную)

## 🔧 Создание ICO файла

### Вариант 1: Онлайн конвертер
1. Перейдите на https://convertio.co/png-ico/
2. Загрузите `favicon-256x256.png`
3. Скачайте `favicon.ico`
4. Сохраните в `docs/assets/favicon.ico`

> **⚠️ Примечание:** Внешняя ссылка на Convertio может стать недоступной или измениться. Рекомендуется предпочитать методы Python или ImageMagick (Варианты 2 и 3), так как они не зависят от внешних сервисов. Если используете онлайн-конвертер, проверьте актуальность URL перед использованием. В качестве альтернативы можно использовать другие проверенные онлайн-конвертеры, такие как CloudConvert (https://cloudconvert.com/png-to-ico) или ICO Convert (https://icoconvert.com/).

### Вариант 2: Python скрипт
```bash
# Установите pillow
uv add pillow

# Создайте ICO из PNG
python -c "from PIL import Image; img = Image.open('favicon-256x256.png'); img.save('favicon.ico', format='ICO', sizes=[(16,16), (32,32), (48,48), (64,64), (128,128), (256,256)])"
```

### Вариант 3: ImageMagick
```bash
magick convert favicon-256x256.png -define icon:auto-resize=256,128,64,48,32,16 favicon.ico
```

## 🔄 Регенерация иконок

Если нужно обновить иконки:

1. Отредактируйте `favicon.svg` (векторная иконка)
2. Запустите генератор:
   ```bash
   python docs/assets/icon-generator.py
   ```
3. Создайте новый `favicon.ico` (см. выше)

## 📝 Использование

Иконки автоматически подключаются через:
- `docs/favicon.html` - мета-теги для всех браузеров
- `mkdocs.yml` - настройка favicon для MkDocs
- `docs/site.webmanifest` - манифест для PWA

## 🎯 Символика элементов

1. **Многослойная структура** - Clean Architecture (Domain, Application, Infrastructure)
2. **Круг (Singleton)** - паттерн Одиночка
3. **Квадрат с крестом (Factory)** - паттерн Фабрика
4. **Треугольник (Observer)** - паттерн Наблюдатель
5. **5 точек (SOLID)** - пять принципов SOLID
6. **Маленькие блоки (Микросервисы)** - распределенная архитектура
7. **Буква "A"** - Architecture (архитектура)

